import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  base: {
    backgroundColor: "#000",
    flex: 1,
  },
  container: {
    paddingTop: 24,
    flex: 1,
    zIndex: 1,
  },
  pauseButton: {
    position: "absolute",
  },
});

export default styles;